---
import type { HTMLAttributes } from 'astro/types';

interface Props extends HTMLAttributes<'a'> {
  children: any;
}

const { ...attrs } = Astro.props;
---

<a
  class="cursor-pointer rounded px-1 hover:bg-neutral-200 hover:text-primary focus:bg-neutral-200 focus:text-primary focus-visible:outline-none dark:hover:bg-neutral-800 dark:focus:bg-neutral-800"
  {...attrs}
>
  <slot />
</a>
